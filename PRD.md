# 고객정보 관리 프로그램 PRD

## 1. 문서 개요

### 1.1 목적

Python의 SQLite3와 PySide를 사용해 고객정보를 등록, 조회, 수정, 삭제할 수 있는 데스크톱 GUI 프로그램을 설계한다.

요구사항과 설계를 기준으로 `MyCust.py`와 `MyCustGUI.py`를 구현하며, `MyCust.db`는 프로그램 실행 시 자동 생성한다.

### 1.2 산출물

| 파일 | 용도 | 이번 단계 |
|---|---|---|
| `PRD.md` | 요구사항 및 설계 문서 | 생성 |
| `MyCust.py` | 데이터베이스 계층 및 실행 진입점 | 생성 완료 |
| `MyCustGUI.py` | PySide6 기반 고객정보 화면 | 생성 완료 |
| `MyCust.db` | SQLite 고객정보 데이터베이스 | 실행 시 생성 |

## 2. 제품 범위

### 2.1 포함 범위

- SQLite3 데이터베이스 연결 및 초기화
- `Customers` 테이블 생성
- 고객정보 입력
- 고객정보 검색
- 선택 고객정보 수정
- 선택 고객정보 삭제
- 하단 `QTableWidget`을 사용한 고객 목록 출력
- 데이터 처리 클래스와 화면 처리 클래스의 분리

### 2.2 제외 범위

- 사용자 로그인 및 권한 관리
- 여러 사용자의 동시 접속
- 고객 사진, 주소, 전화번호 등 추가 정보 관리
- 엑셀 또는 CSV 가져오기/내보내기
- 네트워크 서버 및 원격 데이터베이스
- 복잡한 검색 조건 조합

## 3. 기술 요구사항

- 언어: Python 3
- GUI 프레임워크: PySide 계열. 구현 시 프로젝트 환경에 맞춰 `PySide6`를 우선 사용한다.
- 데이터베이스: Python 표준 라이브러리 `sqlite3`
- 데이터베이스 파일: 프로그램 실행 위치의 `MyCust.db`
- 메인 소스 파일: `MyCust.py`
- 메인 GUI 파일: `MyCustGUI.py`
- 목록 위젯: `QTableWidget`
- 데이터베이스 접근은 SQL 파라미터 바인딩을 사용한다.
- 화면 클래스가 SQL을 직접 실행하지 않도록 한다.

## 4. 데이터베이스 설계

### 4.1 데이터베이스 파일

- 파일명: `MyCust.db`
- 연결 시 데이터베이스가 없으면 SQLite가 파일을 생성한다.
- 프로그램 시작 시 `Customers` 테이블이 없으면 생성한다.

### 4.2 Customers 테이블

```sql
CREATE TABLE IF NOT EXISTS Customers (
    custID INTEGER PRIMARY KEY AUTOINCREMENT,
    custName TEXT NOT NULL,
    custTitle TEXT NOT NULL
);
```

| 컬럼명 | 자료형 | 제약조건 | 설명 |
|---|---|---|---|
| `custID` | `INTEGER` | `PRIMARY KEY AUTOINCREMENT` | 고객 식별자 |
| `custName` | `TEXT` | `NOT NULL` | 고객 이름 |
| `custTitle` | `TEXT` | `NOT NULL` | 고객 직위 또는 호칭 |

### 4.3 정렬 및 조회 기준

- 기본 목록 조회는 `custID` 오름차순으로 한다.
- 검색은 `custName` 또는 `custTitle`에 검색어가 포함되는 고객을 대상으로 한다.
- 검색어가 비어 있으면 전체 고객을 조회한다.

## 5. 화면 설계

### 5.1 기본 화면 구성

메인 화면은 상단 입력/작업 영역과 하단 고객 목록 영역으로 구성한다.

```text
+----------------------------------------------------------+
| 고객정보 관리                                            |
+----------------------+-----------------------------------+
| [입력] [수정]        | 고객 ID: [              ]          |
| [삭제] [검색]        | 고객 이름: [            ]          |
|                      | 고객 직위: [            ]          |
|                      |                                   |
+----------------------+-----------------------------------+
| 고객 목록 (QTableWidget)                                 |
| ID             고객 이름              고객 직위          |
| ...                                                    |
+----------------------------------------------------------+
```

### 5.2 왼쪽 작업 영역

왼쪽에는 다음 버튼을 배치한다.

- `입력`: 오른쪽 입력 컨트롤의 고객정보를 신규 저장한다.
- `수정`: 선택된 고객의 정보를 오른쪽 입력 컨트롤의 값으로 변경한다.
- `삭제`: 선택된 고객을 삭제한다.
- `검색`: 검색어를 기준으로 고객 목록을 다시 조회한다.

버튼은 `QPushButton`을 사용하고, 수직 또는 2열 그리드 형태로 배치한다.

### 5.3 오른쪽 입력 영역

오른쪽에는 다음 입력 컨트롤을 배치한다.

- 고객 ID: `QLineEdit`, 목록 선택 시 표시. 수정/삭제 대상 식별에 사용한다.
- 고객 이름: `QLineEdit`, 입력 및 검색에 사용한다.
- 고객 직위: `QLineEdit`, 입력 및 검색에 사용한다.

고객 ID는 자동 증가 값이므로 신규 입력 시 사용자가 직접 입력하지 않는다. 신규 입력 후 데이터베이스가 생성한 ID를 목록과 입력 영역에 반영한다.

검색 시에는 고객 이름 입력란을 기본 검색어 입력란으로 사용한다. 구현 단계에서 고객 직위까지 함께 검색할 수 있도록 검색 범위를 명확히 표시하거나 검색 전용 입력란을 추가할 수 있다.

### 5.4 하단 목록 영역

하단에는 `QTableWidget`을 배치하고 다음 열을 표시한다.

1. `custID`
2. `custName`
3. `custTitle`

목록 행을 클릭하면 해당 행의 값이 오른쪽 입력 컨트롤에 표시된다. 헤더는 읽기 쉬운 한글 또는 컬럼명으로 표시하며, 컬럼 너비는 내용에 맞게 조정한다.

## 6. 클래스 설계

### 6.1 CustomerManager

데이터베이스 연결, 테이블 초기화, CRUD, 조회 결과 반환을 담당한다. GUI 위젯이나 화면 배치에 의존하지 않는다.

권장 책임:

- 데이터베이스 연결 관리
- `Customers` 테이블 초기화
- SQL 실행 및 커밋
- 조회 결과를 화면에서 사용할 수 있는 형태로 반환
- 데이터베이스 오류를 호출자에게 전달하거나 일관된 예외로 변환

권장 메서드:

```python
class CustomerManager:
    def __init__(self, db_path: str = "MyCust.db"):
        ...

    def create_table(self) -> None:
        ...

    def add_customer(self, cust_name: str, cust_title: str) -> int:
        ...

    def get_customers(self, keyword: str = "") -> list:
        ...

    def update_customer(
        self,
        cust_id: int,
        cust_name: str,
        cust_title: str,
    ) -> bool:
        ...

    def delete_customer(self, cust_id: int) -> bool:
        ...

    def close(self) -> None:
        ...
```

구현 원칙:

- 모든 SQL 값은 문자열 결합이 아니라 파라미터 바인딩으로 전달한다.
- `custID`는 `add_customer`에서 입력받지 않는다.
- 수정 및 삭제 결과가 대상 행에 적용되었는지 반환값으로 알 수 있게 한다.
- 애플리케이션 종료 시 데이터베이스 연결을 닫는다.

### 6.2 CustomerView

PySide 화면과 사용자 이벤트를 담당한다. 데이터베이스 SQL을 직접 작성하지 않고 `CustomerManager` 메서드를 호출한다.

권장 책임:

- 메인 윈도우 및 레이아웃 구성
- 입력 컨트롤과 버튼 생성
- 버튼 클릭 및 테이블 행 선택 이벤트 연결
- 입력값 검증
- `QTableWidget` 갱신
- 성공/실패 메시지 표시
- 화면 종료 시 `CustomerManager.close()` 호출

권장 메서드:

```python
class CustomerView:
    def __init__(self, manager: CustomerManager):
        ...

    def setup_ui(self) -> None:
        ...

    def connect_signals(self) -> None:
        ...

    def load_customers(self, keyword: str = "") -> None:
        ...

    def clear_inputs(self) -> None:
        ...

    def on_add_clicked(self) -> None:
        ...

    def on_update_clicked(self) -> None:
        ...

    def on_delete_clicked(self) -> None:
        ...

    def on_search_clicked(self) -> None:
        ...

    def on_table_row_selected(self, row: int, column: int) -> None:
        ...
```

### 6.3 실행 진입점

`MyCust.py`의 실행부는 다음 흐름을 따른다.

1. `QApplication` 생성
2. `CustomerManager` 생성
3. `CustomerManager.create_table()` 호출
4. `CustomerView` 생성
5. 전체 고객 목록 로드
6. 메인 화면 표시
7. Qt 이벤트 루프 실행
8. 종료 시 데이터베이스 연결 정리

## 7. 기능 요구사항

### FR-01 고객 입력

- 사용자가 고객 이름과 고객 직위를 입력하고 `입력` 버튼을 클릭하면 신규 고객을 저장한다.
- 이름 또는 직위가 비어 있으면 저장하지 않고 안내 메시지를 표시한다.
- 저장 성공 후 목록을 갱신한다.
- 저장된 `custID`는 SQLite 자동 증가 값이어야 한다.

### FR-02 고객 검색

- 검색어를 입력하고 `검색` 버튼을 클릭하면 일치하는 고객만 목록에 표시한다.
- 검색어가 없으면 전체 고객을 표시한다.
- 검색 결과가 없으면 빈 목록과 안내 메시지를 표시한다.

### FR-03 고객 수정

- 목록에서 고객을 선택하면 해당 정보가 입력 영역에 표시된다.
- 사용자가 이름 또는 직위를 수정하고 `수정` 버튼을 클릭하면 선택 고객을 변경한다.
- 고객 ID가 없거나 유효하지 않으면 수정하지 않고 안내 메시지를 표시한다.
- 수정 성공 후 목록을 갱신한다.

### FR-04 고객 삭제

- 목록에서 고객을 선택하고 `삭제` 버튼을 클릭하면 삭제 확인을 요청한다.
- 사용자가 확인한 경우에만 해당 고객을 삭제한다.
- 삭제 성공 후 목록을 갱신하고 입력 컨트롤을 정리한다.

### FR-05 목록 선택

- `QTableWidget`의 행 선택 시 해당 행의 ID, 이름, 직위를 입력 영역에 표시한다.
- 목록은 데이터베이스의 현재 상태와 일치해야 한다.

## 8. 입력 검증 및 오류 처리

- 이름과 직위의 앞뒤 공백은 제거한 뒤 검증한다.
- 이름과 직위가 모두 비어 있지 않아야 한다.
- 숫자가 아닌 고객 ID로 수정/삭제를 시도하면 처리하지 않는다.
- 데이터베이스 오류 발생 시 사용자가 이해할 수 있는 오류 메시지를 표시한다.
- 삭제 전 확인 대화상자를 표시한다.
- 정상 처리 후에는 성공 메시지를 표시하거나 상태 영역으로 결과를 알려야 한다.

## 9. 비기능 요구사항

- 화면과 데이터베이스 처리 로직은 `CustomerView`와 `CustomerManager`로 분리한다.
- GUI 클래스는 SQL 문장을 직접 실행하지 않는다.
- 데이터베이스 연결은 불필요하게 반복 생성하지 않는다.
- 프로그램 재실행 후에도 이전에 저장한 고객정보가 유지되어야 한다.
- 기본 창 크기에서 버튼, 입력 컨트롤, 테이블의 내용이 겹치지 않아야 한다.
- 한글 고객 이름과 직위를 정상적으로 저장하고 표시해야 한다.
- 토스증권 웹페이지 느낌의 깔끔한 디잔인으로 구현이 필요하다.

## 10. 개발 및 검증 기준

구현 완료 후 다음 시나리오를 검증한다.

1. 프로그램을 처음 실행했을 때 `MyCust.db`와 `Customers` 테이블이 생성된다.
2. 고객 1건을 입력하면 목록에 표시되고 `custID`가 자동 생성된다.
3. 프로그램을 종료한 뒤 다시 실행해도 기존 고객이 표시된다.
4. 이름 또는 직위로 검색했을 때 조건에 맞는 행만 표시된다.
5. 목록의 고객을 선택해 이름과 직위를 수정하면 데이터베이스와 목록이 함께 변경된다.
6. 고객 삭제 확인 후 해당 행이 목록에서 사라진다.
7. 빈 값 입력, 잘못된 ID, 데이터베이스 오류가 발생했을 때 프로그램이 종료되지 않고 안내 메시지를 표시한다.

## 11. 완료 조건

- [ ] `PRD.md`에 요구사항과 설계가 정리되어 있다.
- [ ] 구현 단계에서 `MyCust.py`가 생성된다.
- [ ] 구현 단계에서 `MyCust.db`가 생성된다.
- [ ] `Customers` 테이블의 컬럼과 제약조건이 요구사항과 일치한다.
- [ ] `CustomerManager`가 데이터베이스 CRUD를 담당한다.
- [ ] `CustomerView`가 PySide 화면과 이벤트를 담당한다.
- [ ] 왼쪽에 입력/수정/삭제/검색 버튼이 배치된다.
- [ ] 오른쪽에 고객 입력 컨트롤이 배치된다.
- [ ] 하단에 `QTableWidget` 고객 목록이 배치된다.